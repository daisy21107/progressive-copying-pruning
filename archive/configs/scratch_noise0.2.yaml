seed: 42
device: auto
out_dir: runs/scratch_noise0.2

data:
  root: ./data
  batch_size: 128
  num_workers: 0
  label_rule: parity
  pairing: static
  data_seed: 12345
  noise_std: 0.2
  noise_mode: fixed
  noise_seed: 2026

model:
  width: 16
  hidden: 64
  shared_encoder: true

lr: 0.001
epochs: 5
