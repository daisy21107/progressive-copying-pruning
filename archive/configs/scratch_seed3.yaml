seed: 3
device: auto
out_dir: runs/scratch_seed3

data:
  root: ./data
  batch_size: 128
  num_workers: 0
  label_rule: parity

model:
  width: 16
  hidden: 64
  shared_encoder: true

lr: 0.001
epochs: 5
