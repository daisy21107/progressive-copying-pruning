seed: 42
device: auto
out_dir: runs/teacher_convergence_30ep

data:
  task: cifar100_binary_pairs
  root: ./data
  batch_size: 128
  num_workers: 0
  label_rule: parity
  pairing: static
  data_seed: 12345
  noise_std: 0.0
  noise_mode: fixed
  noise_seed: 2026
  label_noise: 0.0

model:
  in_channels: 3
  width: 48 
  hidden: 192 
  shared_encoder: true

lr: 0.0002
epochs: 30
weight_decay: 0.0001

checkpoint_name: teacher.pt