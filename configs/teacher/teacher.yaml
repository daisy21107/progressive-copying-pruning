seed: 42
device: auto
out_dir: runs/teacher

data:
  task: cifar100_binary_pairs
  root: ./data
  batch_size: 128
  num_workers: 0
  label_rule: parity
  pairing: static
  data_seed: 12345

model:
  width: 32
  hidden: 128
  shared_encoder: true

lr: 0.001
epochs: 5
